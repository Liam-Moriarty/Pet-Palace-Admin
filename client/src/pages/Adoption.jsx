import React from "react";
import { AdoptionTable } from "../component";

const Adoption = () => {
  return (
    <section className="w-full h-screen padding">
      <AdoptionTable />
    </section>
  );
};

export default Adoption;
